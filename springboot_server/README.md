# Spring Boot Server - Emotiva Poli

## Ejecutar

```bash
mvn spring-boot:run
```

O compilar y ejecutar:

```bash
mvn clean package
java -jar target/springboot-server-1.0.0.jar
```

La API estará disponible en: http://localhost:6000
